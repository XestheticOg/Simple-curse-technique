'''
<html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Colourful Website</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460);
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    nav {
      width: 100%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 18px 50px;
      background: rgba(255,255,255,0.05);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }

    .logo {
      font-size: 1.6rem;
      font-weight: 800;
      background: linear-gradient(90deg, #e94560, #f5a623, #00d2ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: 2px;
    }

    .nav-links a.secret-link {
      padding: 8px 20px;
      border: 2px solid #e94560;
      border-radius: 25px;
      color: #e94560;
      font-weight: 700;
      letter-spacing: 1px;
      text-decoration: none;
      animation: pulse 2s infinite;
      transition: all 0.3s;
    }
    .nav-links a.secret-link:hover { background: #e94560; color: #fff; }

    @keyframes pulse {
      0%,100% { box-shadow: 0 0 0 0 rgba(233,69,96,0.5); }
      50%      { box-shadow: 0 0 0 10px rgba(233,69,96,0); }
    }

    .hero {
      text-align: center;
      padding: 90px 20px 50px;
    }
    .hero h1 {
      font-size: 3.5rem;
      font-weight: 900;
      background: linear-gradient(90deg, #e94560, #f5a623, #00d2ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 16px;
    }
    .hero p {
      font-size: 1.2rem;
      color: #aaa;
      max-width: 550px;
      margin: 0 auto;
      line-height: 1.7;
    }

    .cards {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 24px;
      padding: 60px 20px;
      max-width: 1000px;
    }
    .card {
      width: 260px;
      padding: 36px 24px;
      border-radius: 20px;
      text-align: center;
      transition: transform 0.3s;
    }
    .card:hover { transform: translateY(-8px); }
    .card:nth-child(1) { background: linear-gradient(135deg, #e94560, #b91c3e); }
    .card:nth-child(2) { background: linear-gradient(135deg, #f5a623, #e68a00); }
    .card:nth-child(3) { background: linear-gradient(135deg, #00d2ff, #0094b3); }
    .card .icon { font-size: 2.8rem; margin-bottom: 14px; }
    .card h3 { font-size: 1.3rem; margin-bottom: 10px; }
    .card p  { font-size: 0.9rem; opacity: 0.85; line-height: 1.6; }

    footer {
      margin-top: auto;
      padding: 24px;
      text-align: center;
      color: #555;
      font-size: 0.85rem;
    }

    .page {
      display: none;
      position: fixed;
      inset: 0;
      z-index: 100;
      background: linear-gradient(135deg, #1a1a2e, #0f3460);
      justify-content: center;
      align-items: center;
      flex-direction: column;
    }
    .page.active { display: flex; }

    .password-box {
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.15);
      padding: 50px 40px;
      border-radius: 24px;
      text-align: center;
      width: 360px;
      backdrop-filter: blur(14px);
      box-shadow: 0 0 60px rgba(233,69,96,0.2);
    }
    .password-box .lock { font-size: 3.5rem; margin-bottom: 16px; }
    .password-box h2 {
      font-size: 1.8rem;
      font-weight: 800;
      margin-bottom: 8px;
      color: #e94560;
    }
    .password-box p { color: rgba(255,255,255,0.55); font-size: 0.9rem; margin-bottom: 28px; }
    .password-box input {
      width: 100%;
      padding: 14px 18px;
      border-radius: 12px;
      border: 2px solid rgba(255,255,255,0.15);
      background: rgba(255,255,255,0.08);
      color: white;
      font-size: 1.1rem;
      text-align: center;
      letter-spacing: 3px;
      outline: none;
      transition: border 0.3s;
      margin-bottom: 18px;
    }
    .password-box input:focus { border-color: #e94560; }
    .password-box .enter-btn {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 12px;
      background: linear-gradient(135deg, #e94560, #f5a623);
      color: white;
      font-size: 1rem;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 6px 20px rgba(233,69,96,0.4);
      transition: transform 0.2s, box-shadow 0.3s;
    }
    .password-box .enter-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(233,69,96,0.7);
    }
    .error-msg {
      color: #e94560;
      font-size: 0.85rem;
      margin-top: 12px;
      min-height: 20px;
    }
    .back-link {
      margin-top: 22px;
      display: inline-block;
      color: rgba(255,255,255,0.4);
      text-decoration: none;
      font-size: 0.85rem;
      transition: color 0.2s;
      cursor: pointer;
    }
    .back-link:hover { color: white; }

    .secret-content {
      text-align: center;
      padding: 40px 20px;
      max-width: 700px;
    }
    .secret-content .trophy { font-size: 5rem; margin-bottom: 20px; animation: bounce 1s infinite alternate; }
    @keyframes bounce {
      from { transform: translateY(0); }
      to   { transform: translateY(-15px); }
    }
    .secret-content h1 {
      font-size: 3rem;
      font-weight: 900;
      background: linear-gradient(90deg, #e94560, #f5a623, #00d2ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 16px;
    }
    .secret-content p {
      font-size: 1.1rem;
      color: rgba(255,255,255,0.75);
      line-height: 1.7;
      margin-bottom: 30px;
    }
    .secret-content .confetti {
      font-size: 2rem;
      letter-spacing: 6px;
      margin-bottom: 30px;
    }
  </style>
</head>
<body>

  <nav>
    <div class="logo">✦ VIVID</div>
    <div class="nav-links">
      <a href="#" class="secret-link" onclick="showPage('password'); return false;">🔒 SECRET</a>
    </div>
  </nav>

  <div id="home-content">
    <div class="hero">
      <h1>Welcome User</h1>
      <p>A colourful space where creativity meets code. Explore freely — but beware, some pages are locked.</p>
    </div>

    <div class="cards">
      <div class="card">
        <div class="icon">🚫</div>
        <h3>Error 404</h3>
        <p>Oops! This page could not be found. It may have vanished into the void.</p>
      </div>
      <div class="card">
        <div class="icon">⚠️</div>
        <h3>Error 404</h3>
        <p>Nothing here but broken links and digital tumbleweeds. Keep looking.</p>
      </div>
      <div class="card">
        <div class="icon">❌</div>
        <h3>Error 404</h3>
        <p>The page you're looking for doesn't exist — or maybe it never did.</p>
      </div>
    </div>

    <footer>© 2025 Vivid. All rights reserved.</footer>
  </div>

  <!-- PASSWORD PAGE -->
  <div class="page" id="password">
    <div class="password-box">
      <div class="lock">🔒</div>
      <h2>TOP SECRET</h2>
      <p>This page is password protected.<br/>Enter the correct password to proceed.</p>
      <input type="password" id="pwdInput" placeholder="••••••••" onkeydown="if(event.key==='Enter') checkPassword()" autofocus/>
      <button class="enter-btn" onclick="checkPassword()">UNLOCK 🔓</button>
      <div class="error-msg" id="errorMsg"></div>
      <span class="back-link" onclick="showPage('home')">← Back to Home</span>
    </div>
  </div>

  <!-- SECRET PAGE -->
  <div class="page" id="secret">
    <div class="secret-content">
      <div class="trophy">🏆</div>
      <div class="confetti">🎉 🎊 🥳 🎊 🎉</div>
      <h1>YOU CRACKED IT!</h1>
      <p>
        Congratulations, Agent! You've successfully unlocked the <strong>Secret Page</strong>.<br/>
        This classified zone is reserved only for the most clever and persistent visitors.<br/>
        You've proven you have what it takes. Welcome to the inner circle! 🌟
      </p>
      <p style="color:rgba(255,255,255,0.4); font-size:0.85rem;">🔑 The password was: <em>attack</em></p>
      <span class="back-link" style="font-size:1rem; color:rgba(255,255,255,0.6);" onclick="showPage('home')">← Return to Home</span>
    </div>
  </div>

  <script>
    function showPage(page) {
      document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
      document.getElementById('errorMsg').textContent = '';
      document.getElementById('pwdInput').value = '';
      if (page !== 'home') {
        document.getElementById(page).classList.add('active');
      }
    }

    function checkPassword() {
      const input = document.getElementById('pwdInput').value.trim();
      const error = document.getElementById('errorMsg');
      if (input === 'attack') {
        showPage('secret');
      } else {
        error.textContent = '❌ Wrong password! Try again.';
        document.getElementById('pwdInput').value = '';
        document.getElementById('pwdInput').focus();
      }
    }
  </script>
</body>
</html>
```
